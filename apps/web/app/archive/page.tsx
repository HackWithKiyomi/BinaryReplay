import { TerminalPage } from "../(terminal)/page-view";
import { discoverHistoricalEventContracts } from "../../lib/dreamdex";
export const dynamic = "force-dynamic";
export default async function Archive(){let markets: Awaited<ReturnType<typeof discoverHistoricalEventContracts>>=[];let error:string|undefined;try{markets=await discoverHistoricalEventContracts(12)}catch(cause){error=cause instanceof Error?cause.message:"Archive discovery unavailable"}return <TerminalPage title="Verified archive" kicker="Historical / finalized">{error?<section className="empty"><strong>Archive discovery is temporarily unavailable.</strong><p>{error}</p></section>:<section className="card" style={{marginTop:20}}><div className="metric"><span>Historical Event Contract windows</span><span>{markets.length}</span></div>{markets.map((market)=><div className="metric" key={market.marketId}><span>{market.symbol} · {market.status}</span><span>{market.resolution.voided?"VOIDED":market.resolution.winningOutcome===null?"unresolved":`winner: ${market.outcomes[market.resolution.winningOutcome]?.label}`}</span></div>)}</section>}</TerminalPage>}
