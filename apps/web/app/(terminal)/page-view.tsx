import Link from "next/link";
export function TerminalPage({title,kicker,children}:{title:string;kicker:string;children:React.ReactNode}){return <main className="page"><p className="eyebrow">{kicker}</p><h1>{title}</h1><p className="lead">Real DreamDEX Event Contract data only. Every record is bound to its immutable marketId.</p>{children}</main>}
export function EmptyData({action,href}:{action:string;href:string}){return <section className="empty"><strong>No verified datasets yet.</strong><p>Connect to Shannon and discover a current Event Contract window. BinaryReplay will not replace unavailable chain data with demo prices.</p><Link className="button" href={href}>{action}</Link></section>}
export function Rules(){return <section className="card section-spaced"><div className="metric"><span>Market identity</span><span>marketId</span></div><div className="metric"><span>Pool address</span><span>non-permanent reference</span></div><div className="metric"><span>Write safeguard</span><span>on-chain lifecycle read</span></div></section>}
