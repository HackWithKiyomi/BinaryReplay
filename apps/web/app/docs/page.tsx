import {TerminalPage,Rules} from "../(terminal)/page-view";
export default function Docs(){return <TerminalPage title="Operator documentation" kicker="How BinaryReplay works"><section className="card" style={{marginTop:20}}><h3>Safe operating sequence</h3><p>Discover the Event Contract through the SDK, capture ordered updates, validate identity and ordering, then replay and simulate. A wallet write is never inferred from indexed data alone.</p></section><Rules/></TerminalPage>}
