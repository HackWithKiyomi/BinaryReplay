# Testing

Run `pnpm validate` for typecheck, security lint, unit/integration tests, and production build. Run `pnpm smoke` separately for read-only Shannon checks. Browser coverage is in Playwright route smoke tests and never automates a private-wallet action. Foundry registry tests require Foundry and forge-std.
