import { describe, expect, it } from "vitest";
import { simulateIntent } from "./index";
const context = { marketId: "m", virtualTime: 1n, expiry: 10n, marketStatus: "Trading" as const, tickSize: 5n, lotSize: 10n, maxSlippageBps: 10_000n, latencyMs: 0n, book: { yesBids: [], yesAsks: [{ price: 50n, quantity: 20n }], noBids: [], noAsks: [] } };
describe("precision and execution guards", () => { it("rejects off-grid price and quantity", () => { expect(simulateIntent({ action: "BUY_UP", quantity: 11n, reason: "x" }, context).status).toBe("REJECTED"); expect(simulateIntent({ action: "BUY_UP", quantity: 10n, limitPrice: 51n, reason: "x" }, context).status).toBe("REJECTED"); }); it("reports no liquidity, lock and expiry", () => { expect(simulateIntent({ action: "BUY_DOWN", quantity: 10n, reason: "x" }, context).status).toBe("NOT_FILLED"); expect(simulateIntent({ action: "BUY_UP", quantity: 10n, reason: "x" }, { ...context, marketStatus: "Locked" }).status).toBe("REJECTED"); expect(simulateIntent({ action: "BUY_UP", quantity: 10n, reason: "x" }, { ...context, virtualTime: 9n, latencyMs: 1n }).status).toBe("EXPIRED"); }); });
